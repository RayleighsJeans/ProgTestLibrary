{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug - Build and debug active file",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}//${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: g++ build active file"
        }
    ]
}